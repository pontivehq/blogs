---
title: Component reference
excerpt: >-
  Every content component available when writing a post, shown in one place —
  what each one looks like, and when to reach for it.
publishedAt: 2026-09-08
draft: true
featured: false
authors:
  - mehdi-soltany
tags:
  - engineering
cover:
  alt: The Pontive blog cover card
seo: {}
---
A reference for everything the editor can insert into a post, rendered exactly
as it appears on the site.

## Callout

<Callout variant="note" title="A note">
  The default. Use it for an aside the reader should not skim past.
</Callout>

<Callout variant="tip">
  A tip, with no title.
</Callout>

<Callout variant="warning" title="Careful">
  Something that will bite if ignored.
</Callout>

## Figure

<Figure alt="The Pontive blog cover card" caption="A figure, with a caption that plain Markdown cannot produce." width="content" />

## Details

<Details summary="Why not use a client-side disclosure?">
  The native element needs no JavaScript, keeps the page a server component, and
  browser find-in-page expands it to reveal a match.
</Details>

## Call to action

<Cta title="Building something that needs all of this?" description="Auth, permissions, subscriptions and billing as one platform." label="Join the waitlist" href="/waitlist" />

## Embed

An unrecognised URL degrades to a link rather than being framed:

<Embed url="https://example.com/not-a-video" title="Not a video" />

## Markdown that needs no component

Tables, task lists and strikethrough all come from `remark-gfm`:

| Feature      | Source      |
| ------------ | ----------- |
| Tables       | remark-gfm  |
| Highlighting | Shiki       |
| Callouts     | a component |

* \[x] Fenced code
* \[ ] ~~Raw HTML~~ (not allowed)

```ts
export const reader = createGitHubReader(config, {
  repo: "pontivehq/blogs",
});
```

And `inline code` inside a sentence.
